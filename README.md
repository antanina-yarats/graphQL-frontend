

#### **`graphQL-frontend/README.md`**
```markdown
# GraphQL Frontend

Frontend project built with vanilla JavaScript to interact with a mock GraphQL API backend. This project works in tandem with the [GraphQL Backend](https://github.com/antanina-yarats/graphQL-backend.git).

## How to Run the Project

1. **Run the Backend**:
   - Navigate to the `graphQL-backend` project folder.
   - Start the backend server with the following command:
     ```bash
     deno run --allow-all app.js
     ```

2. **Run the Frontend**:
   - Open the `index.html` file in a web browser to start the frontend application.

## Technologies Used
- **Frontend**: Vanilla JavaScript
- **Backend API**: GraphQL API

**Note**: The frontend relies on the backend being active for proper functionality.
